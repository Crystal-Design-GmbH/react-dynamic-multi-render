import DynamicMultiRender from 'dynamic-multi-render';
import React from 'react';

interface Props {}

const MainImage = (props: Props) => {
  return <DynamicMultiRender componentName="MainImage" />;
};

export default MainImage;
